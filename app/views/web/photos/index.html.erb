<h1 class="text-center p-3">Photo Albums</h1>

<div class="row">
  <% @albums.each do |album| %>

    <div class="col-lg-4 d-flex justify-content-center">
      <div class="card text-center mb-3" style="width: 18rem; height: 13rem;">
        <div class="card-body">
          <h5 class="card-title"><%= album.title %></h5>
          <h5 class="card-title">Description: <%= album.description %></h5>
          <% if user_signed_in? %>
            <br>
            <%= link_to "Show this Album", web_photos_show_path(id: album.id), class:"btn btn-primary mt-2" %>
          <% end %>
        </div>
      </div>
      </div>
  <% end %>
  </div>
  </div>
</div>