<h1 class="text-center p-3 mb-0">Photos</h1>

<div id="photos">
  <% @photos.each do |photo| %>
    <%= render photo %>
    <p class="mb-0">
      <%= link_to "Show this photo", photo %>
    </p>
  <% end %>
</div>
